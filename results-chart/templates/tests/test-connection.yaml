apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "results-chart.fullname" . }}-test-connection"
  labels:
    {{- include "results-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "results-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
